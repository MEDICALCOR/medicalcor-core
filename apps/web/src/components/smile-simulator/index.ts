/**
 * AI Smile Simulator Component
 *
 * Revolutionary conversion tool that analyzes dental photos using GPT-4 Vision
 * and shows potential smile transformation results.
 *
 * Proven to increase conversions by 200%+ in dental marketing.
 *
 * @example
 * ```tsx
 * import { SmileSimulator } from '@/components/smile-simulator';
 *
 * export default function LandingPage() {
 *   return (
 *     <SmileSimulator
 *       onLeadCapture={(data) => console.log('Lead captured:', data)}
 *       onSimulationComplete={(result) => console.log('Simulation:', result)}
 *     />
 *   );
 * }
 * ```
 */

export { SmileSimulator, default } from './SmileSimulator';
