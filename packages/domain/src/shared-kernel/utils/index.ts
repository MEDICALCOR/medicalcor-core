/**
 * Shared Kernel Utilities
 *
 * Domain-level utilities that are platform-agnostic.
 *
 * @module domain/shared-kernel/utils
 */

export { generateUUID, generateShortUUID, generatePrefixedId } from './uuid.js';
